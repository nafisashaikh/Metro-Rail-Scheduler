
  # Metro Rail Scheduler

  This is a code bundle for Metro Rail Scheduler. The original project is available at https://www.figma.com/design/w9RyQLWTgIeTyPU6ed7TxT/Metro-Rail-Scheduler.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  