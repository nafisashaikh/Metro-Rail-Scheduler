/**
 * Types barrel — re-export all shared TypeScript types.
 * Import: import { User, Alert, MetroLine } from '../types';
 */
export type {
  UserRole,
  SystemSection,
  NetworkType,
  User,
  PassengerUser,
  StationCoord,
  MetroLine,
  Schedule,
  Train,
  TrainHealth,
  TrainCapacity,
  StationMetrics,
  SchedulePrediction,
  Alert,
  WeatherData,
  ForecastDay,
} from './metro';
