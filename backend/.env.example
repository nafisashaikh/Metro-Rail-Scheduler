PORT=4000
JWT_SECRET=change-this-secret-before-production
JWT_EXPIRES_IN=8h
FRONTEND_ORIGIN=http://localhost:5173

# Firebase Admin Configuration (required for Firebase OTP verification in backend)
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=

# Encryption key for sensitive data (must be 32 characters for AES-256)
ENCRYPTION_KEY=your-32-character-encryption-key-here-change-this

# MySQL Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=root123
DB_NAME=metro_rail_scheduler
